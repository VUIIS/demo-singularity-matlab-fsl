#!/usr/bin/env bash
#
# Postprocessing, done after the other parts of the pipeline are run. For this
# example, there is nothing. But any sort of FSL processing etc could be done
# here.

echo Running $(basename "${BASH_SOURCE}")

